# IssuedCardFormFactor

Specifies the type of spend card to be issued. Presently supports virtual only, providing a digital number without a physical card.

## Example Usage

```ruby
require "moov_ruby"

value = IssuedCardFormFactor::VIRTUAL
```


## Values

| Name      | Value     |
| --------- | --------- |
| `VIRTUAL` | virtual   |